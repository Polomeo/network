document.addEventListener('DOMContentLoaded', function () {
    // Page sections button listeners
    // [TODO]


    // Load the lastest 10 posts
    load_posts();

});

function load_posts(user) {

    // if no user is sent, load all post
}